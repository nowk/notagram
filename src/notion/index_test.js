const sinon = require('sinon');

const notion = require('.');

describe('notion', () => {
  // let stub;

  // afterEach(() => {
  //   stub.restore();
  // });

  // it('tests', async () => {
  //   stub = sinon.stub(notion, 'queryDatabase');
  //   stub.returns([{}, null]);

  //   const [data, err] = await notion.queryDatabase();
  //   if (err) {
  //     console.log(err);
  //   }
  //   console.log(data);
  // });

  it.skip('treats status: 400 as an error');
});